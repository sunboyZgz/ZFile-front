import type { Ref } from 'vue'
import { onMounted, onUnmounted } from 'vue'
import { handleClick } from '/#/index'
export const useRightClick = (e: MouseEvent) => {
	// e.preventDefault()
	const style = {}
}
